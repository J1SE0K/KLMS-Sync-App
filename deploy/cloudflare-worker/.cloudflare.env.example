# Cloudflare dashboard에서 만든 API token을 넣는다.
# 권장: "Edit Cloudflare Workers" template 기반 token.
# D1/R2 생성과 lifecycle 설정까지 자동화하려면 Account > D1 > Edit,
# Account > Workers R2 Storage > Edit 권한도 필요하다.
CLOUDFLARE_API_TOKEN=replace-with-cloudflare-api-token
