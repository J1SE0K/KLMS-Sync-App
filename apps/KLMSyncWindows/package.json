{
  "name": "klms-sync-windows",
  "version": "0.1.0",
  "private": true,
  "description": "Windows companion app for KLMS Sync server relay",
  "main": "src/main.cjs",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/dashboard-cache.cjs && node --check src/main.cjs && node --check src/preload.cjs && node --check src/realtime-snapshot.cjs && node --check src/relay-security.cjs && node --check src/relay-state.js && node --check src/renderer.js && node --check playwright.config.js && node --check test/dashboard-cache.test.cjs && node --check test/realtime-snapshot.test.cjs && node --check test/relay-security.test.cjs && node --check test/relay-state.test.cjs && node --check test/ui-quality.test.cjs && node --check test/e2e/electron-entry.cjs && node --check test/e2e/fake-relay.cjs && node --check test/e2e/windows-performance.spec.js && node --check test/e2e/windows-realtime.spec.js",
    "test": "node --test test/*.test.cjs",
    "test:e2e": "playwright test --config playwright.config.js",
    "dist:win": "electron-builder --win nsis --x64",
    "dist:win:arm64": "electron-builder --win nsis --arm64"
  },
  "build": {
    "appId": "com.local.klmssync.windows",
    "productName": "KLMS Sync",
    "artifactName": "${productName} Setup ${version}-${arch}.${ext}",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "assets/**/*",
      "package.json"
    ],
    "win": {
      "icon": "assets/icon.ico",
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "dependencies": {
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "electron": "^43.1.0",
    "electron-builder": "^26.15.3"
  }
}
